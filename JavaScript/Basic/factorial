function factorial(num) {
  let fact = 1;
  if (num >= 0 && num <= 100) {
    for (let i = 1; i <= num; i++) {
      fact *= i;
    }
    return fact;
  }
}
// examples
console.log(factorial(3)); // ===> 6
console.log(factorial(5)); // ===> 120
console.log(factorial(0)); // ===> 1